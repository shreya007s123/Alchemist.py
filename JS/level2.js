// --- LEVEL 2 LOGIC ---
const level2Data = {
    background: "assets/level2/broken_floor.png",
    riddle: "Quick! Write a Python expression that sums three numbers to equal 100."
};

document.addEventListener('DOMContentLoaded', () => {
    document.body.style.backgroundImage = `url('${level2Data.background}')`;
    // Logic for Level 2 math puzzle...
});

// Use your math regex logic here as you did before